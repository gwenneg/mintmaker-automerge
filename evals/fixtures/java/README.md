# java-demo

Quarkus service built by Konflux. Dependency updates come from MintMaker;
Dependabot keeps the Maven dependencies current.
Two Konflux components build `main`, the service and its docs image, and a third builds the `security-compliance` branch.
